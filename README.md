# EasyFlat

A lightweight long-term rental experience inspired by Airbnb, focused on monthly stays in the
MENA region.

## Getting started

Open `index.html` directly in a browser, or run a simple static server:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
